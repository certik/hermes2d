
$(NAME): main.o $(HERMES)
	$(LD) main.o -o $@ $(HERMES) $(LIBS) -lglut -pthread

main.o: main.cpp ../../Make.config ../../include/*.h
	$(CC) $(FLAGS) $(CONFIG) -c $< -o $@ -I../../include $(INCLUDES)

clean:
	rm -f $(NAME) *.o core core.[0-9]*
